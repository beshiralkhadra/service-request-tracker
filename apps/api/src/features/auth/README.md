# Authentication feature

This feature contains registration, login, refresh rotation, logout, bearer
authentication, role middleware, password hashing, JWT issuance, and Prisma
refresh-session persistence. Public registration always creates the Customer
role; Agent accounts are provisioned through the trusted seed process.
