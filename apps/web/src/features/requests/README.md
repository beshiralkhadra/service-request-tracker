# Requests feature

Typed API functions, stable query keys, TanStack Query hooks, and the
feature-local filter store live here. TanStack Query owns server request and
Agent data; Zustand owns only pagination/status filters. This feature also owns
the role-aware dashboard, validated Customer creation form, responsive queue,
detail/history Sheet, Agent assignment/transitions, and live SLA presentation.
